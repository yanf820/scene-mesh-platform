package com.scene.mesh.engin.processor.when;

import com.scene.mesh.foundation.impl.processor.MessageSendProcessor;

public class EventSinker extends MessageSendProcessor {
    @Override
    protected Object[] handleObjectToMessages(Object inputObject) {
        return super.handleObjectToMessages(inputObject);
    }
}
